name 'zooz-puppetdb'
version '0.0.2'
summary "Puppetdb module"
description "This module installs and configures puppetdb. Currently, puppetdb
    is only supported to be run on the same host as puppet master."
